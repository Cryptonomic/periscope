import React from 'react';

interface Props {
    // types goes here
}

const Header: React.FC<Props> = props => {
    return (
        <div>
            side menu
        </div>
    )
}

export default Header;